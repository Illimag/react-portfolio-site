import * as React from 'react';
import { Notification, NotificationProps } from '../Notification';
import { NotificationOption, NotificationsPosition } from './types';
export declare type ToastNotificationProps = NotificationOption & NotificationProps;
export interface ToastNotificationState {
    closing: boolean;
}
interface StyledNotificationProps {
    closing: boolean;
    position?: NotificationsPosition;
}
export declare class ToastNotification extends React.Component<ToastNotificationProps, ToastNotificationState> {
    private autoCloseId;
    private closeId;
    constructor(props: ToastNotificationProps);
    UNSAFE_componentWillMount(): void;
    componentWillUnmount(): void;
    private onCloseHandler;
    private close;
    render(): JSX.Element;
    static inner: {
        readonly StyledNotification: import("styled-components").StyledComponent<typeof Notification, import("../..").PreciseTheme, StyledNotificationProps, never>;
    };
}
export {};
