import * as React from 'react';
import { TextFieldProps } from '../TextField';
import { Omit } from '../../common';
export interface DatePickerTextFieldProps extends Omit<TextFieldProps, 'onChange'> {
    onChange?: React.ChangeEventHandler<HTMLInputElement>;
}
export declare const DatePickerTextField: React.FunctionComponent<DatePickerTextFieldProps> & {
    inner: {
        readonly TextField: React.FunctionComponent<TextFieldProps>;
    };
};
