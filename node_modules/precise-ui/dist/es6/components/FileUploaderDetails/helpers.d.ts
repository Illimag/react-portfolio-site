import { IconName } from '../Icon';
import { FileProgress, ProgressStatus } from './FileUploaderDetails.types.part';
export declare function mergeData(newData: Array<FileProgress>, currentData: Array<FileProgress>): Array<FileProgress>;
export declare const iconNames: {
    [x: string]: IconName;
};
export declare function getStatus(file: FileProgress): ProgressStatus;
